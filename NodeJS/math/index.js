const sum = (x, y) => {
  return x + y;
};

// console.log(sum(10, 10))

const substract = (x, y) => {
  return x - y;
};

// console.log(substract(15, 5));

const multiply = (x, y) => {
  return x * y;
};

// console.log(multiply(5, 5))

const divide = (x, y) => {
  return x / y;
};

// console.log(divide(10, 2))


module.exports = {
    sum,
    substract,
    multiply,
    divide
}

